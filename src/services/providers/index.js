// ─── Active Data Provider ─────────────────────────────────────────────────────
// Change this one import to swap the entire data backend.
// Each provider exposes: chart, quotes, summary, earnings, search,
//                        news, institutional, earningsCalendar

export { default } from './yahoo';
